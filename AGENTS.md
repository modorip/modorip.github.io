# AGENTS.md - mockup/ (모두립 랜딩 페이지)

이 저장소는 Next.js 랜딩 페이지다. 화면 목업·관리 데이터는 `modorip/admin` 에서 관리한다.

**여기 있는 것은 지키지 않으면 조용히 어긋나는 것들이다.** 명령을 돌리면 확인되는 사실(스택·명령·라우트·화면 목록·재사용 자산)은 [TECH.md](./TECH.md)에 있다.

## [중요] 이 저장소는 공개다

`modorip/modorip.github.io` 는 **PUBLIC** 이다. 소스도 커밋 메시지도 주석도 누구나 본다.

- **오픈소스가 아니다.** 소스가 보일 뿐이고 권리는 MILLO 에 있다
- **미패치 취약점·공격 표면·인증 우회를 적지 마라.** 사설 저장소 `../docs/` 에 적는다
  (의존성 보안 추적 → `../docs/06-백엔드/보안-추적.md`)
- **시크릿은 물론이고 내부 URL·포트·계정 구조도 적지 마라**
- README 는 공개 대상이다. 설계 근거는 `../docs/03-디자인/목업-저장소-설계근거.md` 로 옮겼다

### 라우트

| 공개 | 내부 |
|---|---|
| `/` 랜딩 · `/privacy` · `/terms` · `/location` · `/support` | `/overview` `/mockup` `/design` `/icons` `/prototype` `/storyboard` `/licenses` |

내부 라우트는 `public/robots.txt` 가 색인에서 뺀다. [주의] **접근 제어가 아니다.** 주소를
아는 사람은 그대로 보고, 소스로도 드러난다.

**조직 사이트라 `basePath` 가 없다.** 프로젝트 저장소로 옮기면 `/<repo>` 를 넣어야 한다.

## 금지 문자

**문서든 코드 주석이든 화면 문자열이든 쓰지 않는다.** 섹션 기호 U+00A7 · em dash U+2014 ·
전각 문장부호 · 한자·가나 · 이모지.

[중요] **이 저장소는 법적 페이지를 공개한다.** 처리방침·이용약관 본문에 그 문자가 들어가면
그대로 외부에 나간다. 실제로 em dash 26곳이 있었고 그중 여덟이 그 두 화면이었다(2026-08-16).

규칙 정본은 사설 저장소 `docs/협업규칙.md` 의 작성 규칙 절이다. **검사기는 이 저장소를 안
본다** - `docs/tool/validate.py` 가 네 저장소만 걷는다. 여기는 손으로 본다.

```sh
rg -n '\x{2014}' --glob '!node_modules' --glob '!.next'
```

## 법적 페이지 - 외부에 공개된다

`/privacy`(개인정보처리방침) · `/support`(지원)는 **목업이 아니다.** 스토어에 URL 을
제출해 심사자와 이용자가 본다.

- **`src/app/(legal)/` 아래에 둔다.** 라우트 그룹이라 URL 에는 안 나타난다
- [주의] **셸을 라우트 그룹별로 나눴다.** 루트 레이아웃에는 html·body·SEED 설정만
  있고, 읽기 폭 고정은 `(legal)/layout.tsx`, 전면 배경으로 흐르는 랜딩(`/`)은
  `(landing)/layout.tsx` 가 잡는다. **루트로 합치지 마라** - 폭도 배경도 다르다
- [중요] **처리방침에 없는 기능을 적지 마라.** 지키지 못하는 약속이 된다. 기능이
  붙으면 문서와 시행일을 함께 고친다
  - [정정 2026-08-18] 여기 "탈퇴 조항을 넣지 않은 이유가 그것이다(`WITHDRAWN` 이
    코드에 없다)" 고 적혀 있었다. **server V7 이 그 상태를 넣었고**(2026-08-10)
    `DELETE /api/v1/me` 도 구현됐다(08-11). 처리방침 본문은 이미 "서비스 안의
    계정 삭제" 를 적고 있어 **지침만 뒤처져 있었다**
- **위치정보 절이 이 서비스의 핵심 주장이다.** "좌표를 서버로 보내지 않는다"는
  server ADR-0003 이 코드로 보증한다. **그 불변식이 깨지면 이 문장이 거짓이 된다**

### 배포

`.github/workflows/pages.yml` 이 `main` push 마다 GitHub Pages 로 올린다.

- [중요] **저장소가 공개여야 동작한다.** 무료 플랜은 프라이빗에서 Pages 를 제공하지
  않는다. **공개로 바꾸면 목업 화면과 설계 문서(`/design/database` 포함)도 함께
  공개된다.** 켜기 전에 감수할지 정한다
- `output: 'export'` 다. **동적 라우트에 `generateStaticParams` 가 없으면 빌드가
  실패한다**
- `basePath` 는 빌드 때 주입한다. 사용자 지정 도메인을 붙이면 워크플로에서
  `NEXT_PUBLIC_BASE_PATH` 를 지운다. **소스에 경로를 박지 마라**

## 이 저장소의 지침 파일

| 파일 | 담는 것 |
|---|---|
| [AGENTS.md](./AGENTS.md) | **규칙·금지·이식 방침·남은 결함** |
| [TECH.md](./TECH.md) | **스택·명령·TS 함정·라우트·화면 16개·재사용 자산 심볼** |
| [DESIGN.md](./DESIGN.md) | **SEED 적용 규칙.** UI 를 건드리기 전에 읽어라 |
| `../docs/` | **진실의 출처.** 제품·명세·아키텍처 |

Claude Code 는 `CLAUDE.md` 가 `AGENTS.md`·`TECH.md` 를 import 한다. `DESIGN.md` 는 **필요할 때 읽는다**(분량이 커서 매번 싣지 않는다). 개인·휘발성 메모는 `CLAUDE.local.md`(gitignore).

## [주의] 불변식

- **GPS 좌표를 서버로 보내지 않는다**(server ADR-0003). 거리·지오펜스 판정은 단말에서 한다. 목업이라도 이 전제를 깨는 화면을 그리지 않는다
- 서비스 내에 **"한국관광공사"·"KTO" 명칭·로고를 쓰지 않는다**
- **당근 로고·상호명·캐릭터를 앱 화면에 쓰지 않는다.** SEED 는 Apache-2.0 + 당근 상표 조항이고 `/licenses` 라우트가 귀속 고지를 이행한다. **지우지 마라**

## Git

**이 저장소는 브랜치 + PR 로 작업한다.** `main` 에 직접 push 하지 않는다.

정본은 `docs/협업규칙.md` 의 Git 절이다. **2026-08-02 에는 직접 push 대상이었는데**(배포되지 않고 제출물도 아니라서) 저장소 재편으로 **GitHub Pages 공개 배포 대상**이 되면서 2026-08-11 에 옮겼다. 화면 목업 쪽 예외는 `admin` 이 물려받았다.

```sh
git switch main && git pull
git switch -c feature/<slug>
```

커밋 전에 `npx tsc --noEmit && npm run lint && npm run build` 를 직접 통과시킨다(TECH.md 참조).

- **커밋 메시지는 한국어 Conventional Commits.** `type: 요약`(72자 이내, 명령형, 마침표 없음) + 본문 불릿 3~5줄
- **커밋 전 시큐리티 게이트.** `git diff --cached` 로 키·토큰·`.env` 가 섞였는지 확인한다
- 커밋 메시지에 attribution 트레일러 금지

## 명세와 어긋나는 지점

[주의] **`../docs/01-기능명세서` 와 1:1 이 아니다.**

- [주의] **온보딩 5번째 장(이름 입력)이 client 에서 빠졌다(2026-08-06).** client 는 로그인 화면(01B)이 생기면서 이름을 그 뒤(01C)로 옮겼다 - 목업에는 로그인이 없어 이 충돌이 없다. **여기 `slides` 는 그대로 두되, 이름 입력 장을 화면 설계 근거로 쓰지 마라**
- ~~`dex-province`(06)·`dex-sigun-picker`(06C)는 명세에 없는 신규 확장이다~~ **해소(2026-08-02).** 명세가 4단(전국 → 광역 → 시군피커 → 시군상세)으로 재구성됐다. 근거는 `../docs/05-API/분류체계-지역코드.md` 의 계층 규칙
- 명세 `08 3D카드모달` 은 독립 화면이 아니라 `PlaceDetailScreen` 내부에서만 열린다
- [주의] **06 광역의 시트 구성이 client 와 다르다(2026-08-07 대조).** 여기는 인기 자원 레일 · 코스 프리셋 · 시그니처 도시 칩이고, client 는 계열별 진행 카드 · 2열 자원 그리드다. **어느 쪽도 오류가 아니라 데이터 이식 여부에서 갈린 것**이고 갈라진 전체 목록은 명세 06 의 "두 구현이 갈라진 곳" 표가 정본이다
- [주의] **06C 시군 피커의 행별 `{발견}/{총계}` 는 `sigunsOf` 가 `hashStr` 로 지어낸 값이다.** client 는 일부러 이식하지 않았다. **이 수를 화면 설계 근거로 쓰지 마라**

**화면을 바꾸면 명세도 같은 작업에서 갱신하라.**

## Flutter 이식 방침

프레임워크는 Flutter 다. 16개 커스텀 화면·도감·카드·애니메이션 등 공통 UI 비중이 높다. **이 목업이 React 라는 사실은 선택 근거가 아니다**(DOM·CSS 기반이라 React Native 로도 재작성이 필요하다).

- **[주의] 지도는 카카오맵 SDK 로 그린다**(client ADR-0007(지도를 카카오맵 SDK 로 그린다)). 공식 Flutter 플러그인이 없어 서드파티 래퍼를 쓰므로 **네이티브 경계가 지도·위치 권한 둘**이다. 로고·저작권 표기를 지우지 않는다. **근거와 기각한 대안은 그 ADR 이 정본이라 여기 옮겨 적지 않는다** - 이 줄이 결정을 복제하고 있다가 2026-08-07 개정에 뒤처졌었다(원래는 "상용 SDK 를 쓰지 않는다 · 배경을 직접 그린다 · `PlatformView` 가 필요 없다")
- SEED 는 Flutter 런타임 패키지가 없다. `@seed-design/css` 의 light-only 값을 Dart 토큰으로 옮기고 필요한 컴포넌트를 재구현한다. **`/licenses` 상당 화면·NOTICE 고지·제휴 부인도 함께 이식한다**
- 토큰 이식은 끝났다(2026-07-30). client 는 고정 커밋에서 `seed_tokens.g.dart` 를 **생성**한다(client ADR-0002)

client 착수 전 실기기 검증 항목: ① **카카오맵 SDK + 현재 위치 - 핀 300~500개에서 팬·줌이 버티는가**(수도권 밀집 구간이 그 규모다. 서드파티 래퍼 선정 기준이기도 하다) ② 포그라운드·백그라운드 위치 권한과 지오펜스 수명주기 ③ Supabase 카카오·구글·애플 로그인과 딥링크 ④ SEED 토큰을 옮긴 대표 화면 1개(텍스트 확대·스크린 리더).

## 값이 세 저장소에 손으로 복제된다

[주의] **어긋나도 기계가 알려주지 않는다.**

| 값 | 정본 | 사본 |
|---|---|---|
| 브랜드 8토큰 | 여기 `src/app/layout.tsx` 의 `BRAND` 블록 | client `lib/design/tokens/brand_overrides.dart` |
| 4계열 · 17광역 식별색 | admin `src/design/bundle.tsx` 의 `CATEGORY_GROUPS`·`REGIONS` | client `lib/features/catalog/data/catalog_reference_data.dart` · 여기 `src/components/landing.tsx` 의 `CATEGORY_GROUPS`(4계열만) |

생성 파이프라인이 SEED 원본만 읽으므로 **어느 쪽도 자동으로 따라오지 않는다. 바꾸면 세 곳을 함께 고쳐야 한다.**

[주의] **`bundle.tsx` 는 이제 이 저장소에 없다.** 화면 목업과 함께 `admin` 으로 갔다(2026-08-08).
랜딩이 4계열 색을 쓰면서 세 번째 사본이 생겼고, 17광역은 랜딩에 없다.

- **[주의] 21개 2차 카테고리는 client 로 이식되지 않았다(2026-08-04, 확정).** `bundle.tsx` 의 `CATEGORIES` 는 화면 설계를 위해 지어낸 축이고 **KTO 데이터가 그 축을 지탱하지 않는다.** 서버는 신분류 중분류 `lclsSystm2` 23개를 쓰며 양방향으로 어긋난다. `계곡·폭포` 와 `호수·강` 은 KTO 에서 둘 다 `NA02` 라 나눌 수 없고, 반대로 문화는 KTO 가 11개로 더 잘게 쪼갠다. 근거와 전체 대조는 **client ADR-0010**
  - **여기 `CATEGORIES` 는 그대로 둔다.** 목업은 제출물이 아니라 화면 설계 레퍼런스이고, 칩 레이아웃·아이콘 선택은 여전히 참고 자산이다
  - [주의] **다만 이 축으로 필터 화면을 더 설계하지 마라.** 실제 앱에서 그 칸들은 존재하지 않는다
- **[주의] 지금 17광역 값이 갈라져 있다(2026-08-04, 의도적).** 여기 `REGIONS` 는 `tone`·`dark`·`soft` 3역할 새 값이고 client 는 구 단일 `tone` 이다. client 이식은 담당자 협의 대기다
- **역할을 뒤집어 쓰지 마라** - `tone` 에는 대비 하한이 없어 글자에 쓰면 안 읽힌다. 근거는 `../docs/03-디자인/디자인시스템.md` 의 "17광역 식별색 - 확장" 절
- **간격 토큰 드리프트는 없다(2026-07-31 실측).** `spacingX.globalGutter` 가 mockup 20px / client 16px 로 어긋난다는 경고가 있었으나 사실이 아니었다. 지금은 양쪽 모두 SEED 원본 `x4`(16px)다

## 목데이터를 믿을 수 있는 범위

- **`PLACES` 81개는 분포 편향이 심하다.** 제주가 절반 가까이, 광주·세종·울산·대구·대전 0개. `r.total = Math.max(list.length, 12)` 가 지역 총계를 인위 보정하고 있어 **서버 카운트로 교체해야 한다**
- **좌표는 실측값이고 투영은 보정된 근사다(2026-08-03).** `geoToSvg` 는 광역시 7곳의 path bbox 중심 ↔ 시청 좌표로 최소제곱 보정한 선형 변환이며 잔차 RMS 는 6.8 SVG 단위(약 4km)다. 거리 판정은 투영을 거치지 않고 `haversineMeters` 로 실좌표에서 직접 재므로 투영 오차와 무관하다
- [주의] **핀이 바다 위에 찍히는 건 알려진 한계이고 mockup 에서 고치지 않는다(2026-08-03 결정).** 원인은 계산 실수가 아니라 **핀은 실좌표인데 배경 그림은 출처가 다르다**는 것이다. `KOREA_PATHS` 는 `southKoreaHigh.svg` 에서 뽑은 것이라 어떤 투영인지 알 수 없다. 해안가 자원은 몇 백 미터 밀려 바다에 뜬다
  - **client 에는 이 문제가 없다.** 카카오맵 SDK 가 배경과 투영을 함께 맡아 핀과 배경이 어긋날 자리가 없다(client ADR-0007). **목업 지도의 핀 위치를 실제 위치의 근거로 쓰지 마라**
- [주의] **`haeinsa`(해인사)는 `region: 'gyeongbuk'` 인데 실제 위치는 경남 합천이다.** 좌표가 실측값이라 발견 화면에서 경남에 찍힌다. 도감 지역 분류를 고칠지 좌표를 맞출지 미결이다

## 디자인 - 상세는 [DESIGN.md](./DESIGN.md)

- **스타일 소스는 `@seed-design/css` 하나뿐이고 자체 CSS 는 SEED 토큰 재정의 1블록 외 0개다.** `.css` 파일 추가·자체 `--*` 토큰 선언 금지. `<style>` 은 `layout.tsx` 의 `RESET`·`BRAND` 2개가 전부이며 **늘리지 않는다**
- **브랜드는 청색이고 역할이 나뉘어 있다.** 바탕 `bg.brand-solid`, 글자·아이콘 `fg.brand`. **뒤집어 쓰지 마라.** 값과 근거는 DESIGN.md 의 "브랜드색 - 확정" 절
- [주의] **17광역은 현행 행정구역이 아니다.** 2026-07-01 에 광주·전남이 전남광주통합특별시로 통합돼 실제로는 16개다. **도감 구조를 위해 17을 유지한다**(server ADR-0012). mockup `REGIONS` 도 17 그대로 두고, **여기서 통합을 표현하려 하지 마라** - 서버가 시군구 코드로 갈라 준다
- **4계열·17광역 식별색은 디자인 토큰이 아니라 도메인 데이터다.** `bundle.tsx` 가 정본이고 raw hex 로 둔다. **토큰으로 옮기지 마라**
- **새 UI 는 SEED 에 있는지 먼저 확인**하고 없을 때만 `Box` 로 조립한다. 조사 순서를 건너뛰면 있는 걸 없다고 단정한다(전례 있음). 조립할 때도 색·간격·라운드는 SEED 토큰만 참조한다
- 플랫폼 중립 디자인 결정과 근거는 `../docs/03-디자인/디자인시스템.md`

## 남은 결함

[중요] **이 저장소는 공개다.** 미패치 취약점·공격 표면·인증 우회 같은 서술을 여기 적지 마라.
읽는 사람이 팀만이 아니다. 그런 내용은 **사설 저장소 `docs/` 에 적는다.** 의존성 보안
추적은 `../docs/06-백엔드/보안-추적.md` 로 옮겼다(2026-08-08).

- **`npm audit fix --force` 를 실행하지 마라.** Next·ESLint 를 강제 다운그레이드한다

## 주의

- **화면을 바꾸면 `../docs/01-기능명세서/` 도 갱신한다.** 06 계층 확장은 반영됐다(명세 2026-08-02, 화면 구조 대조 2026-08-07)
- 저장소 간 참조는 링크 대신 **저장소명 + 번호 + 요지**로 적는다. 멀티레포라 상대 경로가 깨진다
- 문서 언어는 한국어
- 자체 토큰 → SEED 치환 이력(매핑표·치환 통계)은 이 문서에서 뺐다. 필요하면 git 히스토리(2026-07-28~29 커밋)를 보라
